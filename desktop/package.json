{
  "name": "paper-radar-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for Paper Radar",
  "author": "Paper Radar",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build:frontend": "npm --prefix ../frontend run build",
    "build": "npm run build:win",
    "build:win": "npm run build:frontend && electron-builder --win --dir",
    "build:mac": "npm run build:frontend && electron-builder --mac --dir"
  },
  "build": {
    "appId": "com.paperradar.desktop",
    "productName": "Paper Radar",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "asar": true,
    "win": {
      "icon": "assets/icon.ico"
    },
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.icns",
      "target": [
        "dir"
      ]
    }
  },
  "devDependencies": {
    "electron": "^41.7.1",
    "electron-builder": "^26.15.3"
  }
}
